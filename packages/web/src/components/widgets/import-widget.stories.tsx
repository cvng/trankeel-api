import React from "react";
import { ImportWidget } from "./import-widget";

export default {
  title: "Dashboard/ImportWidget",
  component: ImportWidget,
};

export const importWidget = () => <ImportWidget />;
