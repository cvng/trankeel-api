[package]
name = "trankeel-graphql"
version = "0.6.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
async-graphql = { version = "3.0.36", features = ["apollo_tracing", "tracing"] }
serde = "1.0"
trankeel = { path = "../lib" }
