{
  "name": "piteo",
  "version": "0.1.0",
  "private": "true",
  "scripts": {
    "dev": "cargo watch -i schema.graphql -x run",
    "build": "cargo build",
    "start": "cargo run",
    "lint": "cargo clippy",
    "test": "cargo test",
    "fmt": "cargo fmt",
    "setup": "diesel setup && cargo run -p piteo-cli -- seed",
    "generate": "cargo run -p piteo-cli -- generate"
  }
}
