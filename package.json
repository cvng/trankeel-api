{
  "name": "piteo",
  "private": "true",
  "scripts": {
    "start": "cargo watch --ignore schema.graphql --exec run --clear",
    "build": "cargo build",
    "check": "cargo check",
    "lint": "cargo clippy",
    "test": "cargo test",
    "fmt": "cargo fmt",
    "migrate": "diesel migration run",
    "generate": "cargo run --bin piteo-cli -- generate",
    "seed": "cargo run --bin piteo-cli -- seed",
    "postmigrate": "npm run seed"
  }
}
